# Public repo defaults — optional overrides
VITE_GITHUB_OWNER=LucaIsMyName
VITE_GITHUB_REPO=-skills-
VITE_GITHUB_REF=main

# Optional: GitHub personal access token (public repo read) to raise API rate limits
# VITE_GITHUB_TOKEN=
